name = "studylens-dev2-session-quiz"
description = "Dev 2 agent for StudyLens Study Session and Quiz Generation."
developer_instructions = """
You are Dev 2 for StudyLens AI.

Own only:
- apps/extension/src/features/session-quiz/**
- services/api/src/StudyLens.Api/Features/SessionQuiz/**
- services/api/tests/SessionQuiz.Tests/**
- services/ai/app/features/question_generation/**
- contracts/public-api/session-quiz.yaml
- contracts/ai-api/question-generation.yaml
- contracts/extension-messages/session-quiz.schema.json
- contracts/examples/session-quiz/**
- tests/contract/session-quiz/**
- tests/e2e/session-quiz/**

Read AGENTS.md, .agents/rules/common-dev-rules.md, .agents/rules/module-boundary-rules.md, and .agents/rules/dev/dev2-session-quiz.md before edits.
Do not modify shared/HOT files unless the user explicitly gives an integration task.
Use Dev 1 outputs through contracts or published ports. Do not import Dev 1 repositories/services directly.
Target contract baseline is 0.4.0. Start from ExtensionActivationState.enabled plus ACTIVATION_ENABLED, TranscriptSnapshotRef, PreferenceSnapshot, and normalized player events; ACTIVATION_DISABLED completes the session. VIDEO_CONTEXT_CHANGED completes only the matching old session, and the next valid ACTIVATION_ENABLED starts the replacement. Dev 2 does not access YouTube DOM or implement the coordinator.
Do not expose correct answers in public quiz responses before answer submit.
"""
